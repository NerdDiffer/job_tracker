<% hidden_field_tag('category_names', params[:category_names]) %>
<% tags = options_for_select(
     generate_select_opts_for_category_names,
     params[:category_names]
) %>
<% opts = { multiple: true, size: 10 } %>
<%= select_tag('category_names', tags, opts) %>
