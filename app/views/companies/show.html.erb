<% @page_title = @company.name %>

<h1><%= @company.name %></h1>

<% if logged_in? %>
  <p>
    <%= link_to 'Back', companies_path %> |
    <%= link_to('Edit', edit_company_path(@company)) %>
  </p>
<% else %>
  <p>
    <%= link_to 'Back', companies_path %>
  </p>
<% end %>

  <strong>Website:</strong>
  <% website = @company.website %>
  <%= link_to(
    website,
    interpolate_link(website),
    target: '_blank'
  ) %>
</p>

<p>
  <h2>Categories</h2>
  <% categories = @company.categories %>
  <% categories.sorted.each do |category| %>
    <li><%= link_to(category.display_name, category_path(category)) %></li>
  <% end %>
</p>

<% if logged_in? %>
  <p>
    <%= link_to('Edit Categories', edit_company_path(@company)) %>
  </p>
  <% locals = { company: @company,
                contacts: @contacts,
                job_applications: @job_applications } %>
  <%= render(partial: 'user_attached_records', locals: locals) %>
<% end %>
