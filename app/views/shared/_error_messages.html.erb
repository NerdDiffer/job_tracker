<% if curr_object && curr_object.errors.size > 0 %>
  <div id="error_explanation">
    <h2><%= pluralize(curr_object.errors.size, "error") %> prohibited this record from being saved:</h2>
    <ul>
    <% curr_object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
